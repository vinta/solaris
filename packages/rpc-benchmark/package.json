{
  "name": "@solaris/rpc-benchmark",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "sls deploy -s prod --verbose",
    "deploy:fast": "sls deploy function -s prod -f optimism",
    "invoke:optimism": "sls invoke -s prod -f optimism --log",
    "invoke:local:optimism": "sls invoke local -s dev -f optimism",
    "logs:optimism": "sls logs -s prod -f optimism --startTime 1h -t"
  },
  "dependencies": {
    "@solaris/common": "*"
  },
  "devDependencies": {
    "esbuild": "0.25.11",
    "serverless": "3.40.0",
    "serverless-deployment-bucket": "1.6.0",
    "serverless-esbuild": "1.55.1"
  }
}
